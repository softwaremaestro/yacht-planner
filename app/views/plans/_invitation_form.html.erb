<%= simple_form_for(@planInvitation, :html => {:class => 'form-horizontal' }) do |f| %>
    <%= f.input :plan_id, :as => :hidden, :input_html => { :value => plan.id }  %>
    <%= f.button :submit, value: "전송", :class => "" %>
<% end %>